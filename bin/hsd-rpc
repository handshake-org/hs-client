#!/usr/bin/env node

const CLI = require('./hsd-cli');

(async () => {
  const cli = new CLI();
  await cli.rpc();
  await cli.destroy();
})().catch((err) => {
  console.error(err.stack);
  process.exit(1);
});
